# Update the ONYX logo

## Scope
- Store the uploaded ONYX artwork as a project asset.
- Replace the placeholder “O” tile in the shared logo and sign-in page with the new artwork while keeping existing sizing and labels.
- Create a lightweight favicon from the same artwork and update the site icon reference.
- Verify the app compiles and the logo displays correctly in the sign-in page and navigation.

## Technical details
- Use the CDN asset pointer for in-app rendering.
- Keep the favicon as a resized real image under `public/` for browser compatibility.
- Do not alter application behavior, routes, themes, or data access.
